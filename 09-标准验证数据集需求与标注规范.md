# 深度伪造识别标准验证数据集需求与标注规范

> 文件性质：标准验证数据集建设、标注、质检、分区和交付的技术规范草案。
>
> 适用阶段：D0 开发、D1 探索、D2 校准、D3 独立确认。
>
> 配套文件：`07-能力体系与指标体系汇总报告.md`、`08-指标计算方法与等级阈值校准方案.md`。
>
> 版本状态：v0.1，供数据试制、实验验证和专家评审使用。涉及样本规模、扰动强度、容差和一致性阈值的数值均须经 D1/D2 实验校准后冻结。

## 1. 目的与适用范围

本规范规定深度伪造识别能力评估所用标准验证数据集的建设要求，包括数据来源、样本构成、分类编码、真值标注、数据分区、去重防泄漏、质量控制、安全合规、版本管理和交付结构。

本规范服务于以下目标：

1. 为 T0—T9 测试任务和 39 项评价指标提供可计算、可审计的输入和真值；
2. 将 M1—M5 模态组、O1—O4 对象组、R1—R8 扰动组和 G1—G4 泛化组转化为可执行的数据规则；
3. 保证 D0—D3 的用途、权限和证据强度相互隔离；
4. 支撑指标、权重、门槛和 L1—L4 等级阈值的探索、校准与独立确认；
5. 保证数据许可、标注依据、处理过程和版本变更可追溯。

本规范不规定具体检测模型的训练方法，不把公开数据集的既有标签自动视为标准真值，也不预先固定尚未完成实验校准的等级阈值。

## 2. 规范性术语

### 2.1 样本与源实体

| 术语 | 定义 |
|---|---|
| 媒体样本 | 作为一次测试输入的视频、音频、图像、文本或其组合。 |
| 源媒体 | 生成、裁剪或扰动测试样本之前的原始媒体实体。 |
| 来源组 | 在身份、拍摄事件、源视频、生成过程或传播链路上高度相关的一组样本。 |
| 独立样本单位 | 用于样本量估计和统计重采样的最小独立实体，默认不低于源视频层级。 |
| 派生样本 | 由同一源媒体经伪造、裁剪、转码、扰动或封装得到的样本。 |
| 近重复 | 内容在感知、时序或语义上高度相似，可能造成训练、调参和测试信息泄漏的媒体。 |

### 2.2 真值与标注

| 术语 | 定义 |
|---|---|
| 真值 | 经来源证据、生成记录、人工标注和复核形成的标准答案。 |
| 原生真值 | 由受控采集、受控生成或可验证制作流程直接产生的真值。 |
| 推定真值 | 根据公开数据说明、人工判断或间接证据得到，仍存在不确定性的标签。 |
| 作用域 | 伪造在时间、空间和语义对象上的作用范围，由对象类别、空间部位、时间范围和篡改粒度共同描述。 |
| 证据标注 | 支撑真伪判断、方法判断或定位判断的可复核依据及其位置。 |
| 仲裁 | 两名独立标注员结果不一致时，由第三方或领域专家作出的记录化处理。 |

### 2.3 已知与未知

“已知”表示相应方法族或作用域类别可在被测对象的训练、调参、阈值选择或系统适配阶段出现；“未知”表示在上述全部阶段严格留出。仅隐藏具体样本、但保留同一生成方法族、同源模型、同一作用域类别或其近重复内容，不构成未知条件。

“未知”是相对于一次冻结评测协议和被测对象知识边界定义的属性，不是样本永久不变的固有属性。因此，`method_known` 和 `scope_known` 应写入评测分区清单，不应作为脱离协议的静态事实使用。

## 3. 总体建设原则

### 3.1 代表性与风险覆盖

数据集应覆盖真实与伪造、多种模态、多类对象、不同质量和传播条件。O2 物体目标、O3 场景、O4 文字、M4 文本和 M5 多模态为现有公开数据常见缺口，应在标准验证数据中专项补强。

代表性不等于追求单一总体分布。门槛指标、重要目标、G4 开放组合和稀缺子组应具备单独分析所需的独立样本，不能被多数类平均结果掩盖。

### 3.2 可追溯性

每个样本应能追溯至来源、权利状态、源实体、生成或编辑过程、标注记录、质检记录、分区版本和文件哈希。无法建立最低证据链的样本不得进入 D2 和 D3。

### 3.3 独立性与防泄漏

分区应以来源组为原子单位。同一源视频、同一身份、同一事件、同一生成链路、近重复内容以及由同一源媒体产生的全部扰动版本不得跨 D0—D3 或跨训练、调参、校准、确认边界。

### 3.4 配对和单因素控制

鲁棒性样本应保留干净基准与配对扰动版本。一次扰动实验原则上只改变一个关键因素；复合扰动应另行编码，不得冒充单因素结果。

### 3.5 标签最小披露

D3 在冻结评测方案前应隐藏标签或由独立数据保管人员管理。向被测对象提供的数据只应包含运行所必需的媒体和公开元数据，不得披露可推断真值、方法族、作用域或分组答案的信息。

### 3.6 可复现与不可变交付

每个正式数据版本应具有不可变样本清单、标注版本、分区清单、计算机可读数据卡和哈希清单。对已发布版本的任何增删替换都应形成新版本，不得原位覆盖。

### 3.7 人工判断可审计

主观标注应记录标注员、时间、工具、依据、置信度、冲突和仲裁结果。对重要人物、武器装备、场景语义和文字语义的判断应增加领域专家复核。

## 4. 数据体系与分区

### 4.1 数据来源类型

| 编码 | 类型 | 主要用途 | 进入正式评测的条件 |
|---|---|---|---|
| SRC-PUB | 公开参考数据 | 流程打通、研究对齐、外部泛化 | 许可允许、版本可固定、标签可复核、完成去重 |
| SRC-CTL | 受控采集数据 | 真实样本、源媒体和场景控制 | 有采集协议、授权、设备与环境记录 |
| SRC-GEN | 受控生成数据 | 方法族、参数和作用域真值 | 保留生成工具、模型、参数、输入和日志 |
| SRC-EXT | 专项扩展数据 | 重要目标、物体、场景、文字 | 领域复核、访问控制和用途边界明确 |
| SRC-WLD | 真实传播链路数据 | 平台转码、复杂退化和在野场景 | 来源合法、隐私处理完成、真值证据足够 |

公开数据可作为标准验证数据的一部分，但不得作为唯一来源。正式能力等级使用的数据应做到版本固定、许可明确、构成可控和标注可审计。

### 4.2 D0—D3 分区

| 分区 | 用途 | 可见范围 | 允许操作 | 禁止操作 | 最低管理要求 |
|---|---|---|---|---|---|
| D0 开发集 | 接口联调、指标边界测试、错误修复 | 可查看媒体和全部标签 | 调整脚本、修复格式、构造边界样例 | 用于正式阈值或等级证据 | 版本、哈希、已知问题清单 |
| D1 探索集 | 分析分布、区分度、冗余和样本量 | 研究组可按权限查看标签 | 比较指标与候选方案 | 作为最终确认数据 | 完整标注、来源组清单、探索记录 |
| D2 校准集 | 冻结归一化、权重、门槛和候选阈值 | 限定校准人员可见 | 按预注册方案选择候选参数 | 修改 SUT；结果后无限增加候选 | 预注册、操作审计、冻结清单 |
| D3 独立保留集 | 一次性确认冻结方案 | 标签托管方可见；执行方默认不可见 | 一次性运行并生成确认报告 | 查看结果后继续调阈值并复用本版 D3 | 独立保管、双人授权、解封记录 |

### 4.3 分区状态机

```text
候选样本
  → 权利与安全审查
  → 技术质检
  → 双人标注与仲裁
  → 来源组归并和全库去重
  → 分层统计与覆盖审查
  → 分区冻结
  → 生成不可变清单和哈希
  → 按权限发布或托管
```

样本从较低证据等级升级至 D2/D3 时，应重新执行权利审查、标注复核、全库去重和分区冲突检查。D3 一旦解封，不得再降级为新的独立确认集继续使用。

### 4.4 分区原子组

以下实体应生成稳定的 `group_id`，并整体分配到同一分区：

- 同一源视频及其全部帧、片段、裁剪和封装；
- 同一源媒体产生的真实/伪造配对及全部扰动版本；
- 同一人物或敏感主体的高度相关素材；
- 同一拍摄事件、连续拍摄序列或同一语义事件；
- 同一模板、驱动源、目标源和生成任务产生的批次；
- 经感知哈希、指纹或人工复核判定的近重复内容。

如身份级隔离与事件级隔离产生冲突，应采用更高层级、覆盖范围更大的原子组。

## 5. 样本分类与覆盖要求

### 5.1 真伪类别

| 类别 | 定义 | 纳入要求 |
|---|---|---|
| REAL | 未实施评价范围内伪造的媒体 | 来源可信；允许常规编码、剪辑或字幕，但应记录处理历史 |
| FAKE | 至少一个评价模态或作用域被人工生成、替换、重演或语义篡改 | 有受控生成记录或足够证据；标出伪造模态、对象、方法和作用域 |
| UNCERTAIN | 真值证据不足或存在不可消除争议 | 不进入主指标；可进入争议样本库和标注研究 |
| OUT-OF-SCOPE | 仅包含标准范围外编辑或质量问题 | 不进入主指标；用于边界测试时单独报告 |

正常剪辑、色彩校正、合法字幕叠加和常规转码不应仅因“被编辑”自动标为 FAKE。数据集应在标注指南中明确“深度伪造”“传统编辑”“内容错误”和“媒体退化”的边界。

### 5.2 M1—M5 伪造模态组

| 组别 | 样本定义 | 必需真值 | 典型测试接口 |
|---|---|---|---|
| M1 仅音频伪造 | 视频视觉保持真实，音轨存在声音克隆、换声或局部篡改 | 音频真伪、时间片段、方法族；视觉为真实 | T2、T4、T8 |
| M2 仅视频伪造 | 音频保持真实或无音频，视觉存在换脸、重演、唇形或动作伪造 | 视觉真伪、片段/帧/区域、方法族 | T2、T4 |
| M3 图像/帧级伪造 | 视频帧内存在局部生成、目标替换或帧级篡改 | 受影响帧、空间区域、对象和方法 | T2、T3、T4 |
| M4 文本伪造 | 字幕、画面文字、编号、标牌或屏幕文本被生成或篡改 | 文本区域、原/改文本、语义影响、片段 | T2、T3、T4、T8 |
| M5 多模态混合伪造 | 两种及以上模态被伪造，或模态之间存在人为制造的不一致 | 各模态真值、冲突关系、位置和证据 | T2、T4、T8 |

无音频视频不得默认归入 M1；静态图像嵌入视频时应同时记录载体媒体和被篡改模态。一个样本可拥有多个模态标签，但用于互斥分组统计时应按预注册优先规则归入单一主组，并保留完整多标签。

### 5.3 O1—O4 伪造对象组

| 组别 | 对象范围 | 最低标注要求 | 专项注意事项 |
|---|---|---|---|
| O1 人脸 | 身份、表情、口型、面部局部 | 对象轨迹、区域、身份是否敏感、伪造类型 | 不以身份识别结果替代伪造真值 |
| O2 物体目标 | 武器装备、车辆、设备、标识物和其他实体 | 类别、位置、可见性、遮挡、语义重要性 | 重要目标须领域专家复核 |
| O3 场景 | 背景、地点、环境、天气和事件场景 | 场景类别、受影响范围、时段、语义变化 | 区分场景生成与普通后期处理 |
| O4 文字 | 字幕、标牌、编号、时间地点和屏幕文字 | 文本区域、转写、语言、原/改内容和语义影响 | OCR 不确定性应单独记录 |

多对象样本应逐对象标注，并指定 `primary_object` 供分层抽样使用。无法精确定位但能确定对象类别的样本可用于对象级分类，不得用于空间定位主指标。

### 5.4 伪造方法与方法族

`fake_method` 记录具体方法或工具，`method_family` 记录能够支撑泛化留出的上位类别。方法族至少应区分：

- 身份替换；
- 表情、姿态或动作重演；
- 口型或视听同步操控；
- 全帧或视频生成；
- 局部图像编辑或修复；
- 目标插入、删除或替换；
- 场景或背景生成；
- 语音克隆、转换或音频编辑；
- 文本生成或文字区域篡改；
- 多模态联合生成或组合编辑。

具体分类表应保留 `other` 和 `unknown`，但二者含义不同：`other` 表示已知存在、尚未形成独立类别；`unknown` 表示证据不足，不能推定。用于 G3/G4 的未知方法组必须在方法族层级留出，不能只留出工具版本或随机种子。

### 5.5 伪造作用域

作用域由以下字段联合定义：

| 维度 | 建议值 |
|---|---|
| `fake_object` | face / object / scene / text |
| `fake_region_type` | facial_local / object_local / background_local / text_region / full_frame / audio_track / other |
| `temporal_extent` | instant / segment / intermittent / full_duration |
| `granularity` | local / object / scene / full_generation |
| `spatial_representation` | none / bbox / polygon / mask / track |

G2/G4 的未知作用域应至少在上述一个预注册类别上严格留出，并说明留出层级。只改变坐标位置、框大小或出现时间，不足以自动构成未知作用域。

### 5.6 应用场景与自然质量分层

样本应记录离线取证、批量审查、实时识别、重要目标识别等应用场景。自然质量至少按分辨率、时长、帧率、编码、码率、音频采样率、语言、光照、遮挡、拍摄设备和传播代际分层。自然质量属性与人工 R1—R8 扰动必须分别记录，避免把原生低质样本误作受控扰动样本。

## 6. R1—R8 扰动样本规范

### 6.1 共同要求

每个受控扰动样本应具有唯一的干净基准 `parent_sample_id`，继承真值，并记录完整变换参数、工具版本、随机种子和输出哈希。轻、中、重三级的具体数值在 E5 中校准，在 D2 前冻结。

扰动强度应同时满足：

- 轻度能够模拟常见传播处理且内容仍可正常使用；
- 中度形成可测性能梯度，但不改变真值；
- 重度接近目标场景可接受质量下限，而不是把内容破坏到人类无法判断；
- 同级不同扰动的感知强度可通过客观质量指标和人工复核共同校准；
- 若扰动改变可见真值或使证据不可判定，应标为无效或另建复合场景。

### 6.2 扰动编码表

| 组别 | 内容 | 参数记录要求 | 主要无效条件 |
|---|---|---|---|
| R1 视频压缩 | H.264/H.265 等编码质量变化 | 编码器、profile、QP/CRF或目标码率、GOP、像素格式 | 解码失败；严重破坏真值 |
| R2 重编码/封装 | 编解码器转换、容器变化和传播代际 | 输入/输出编码、容器、转码次数、参数链 | 与 R1 无法区分且未声明复合处理 |
| R3 分辨率变化 | 缩放、上采样和下采样 | 原/目标尺寸、缩放算法、纵横比处理 | 非预期裁剪或形变改变作用域 |
| R4 噪声/模糊/色彩 | 高斯噪声、运动模糊、亮度和色彩退化 | 噪声分布、核、强度、颜色参数、随机种子 | 局部遮挡真值或强度不可复现 |
| R5 裁剪/遮挡/叠加 | 空间裁剪、贴纸、界面元素或遮挡 | 裁剪框、遮挡掩膜、面积比、位置、叠加资源哈希 | 伪造区域完全消失却仍沿用原真值 |
| R6 帧率/时序 | 降帧、插帧、丢帧或时间采样变化 | 原/目标帧率、采样规则、时间映射 | 时间标注未重映射；音画时长失配未记录 |
| R7 音频退化 | 噪声、重采样、编解码和带宽限制 | SNR或强度、采样率、编码、声道、时间映射 | 音频被删除；片段真值无法对齐 |
| R8 对抗扰动 | 面向检测系统的小幅定向或非定向扰动 | 威胁模型、攻击算法、预算、迭代、知识条件、成功判据 | 未限定攻击能力；只报告成功样本 |

R8 应单独声明白盒、灰盒或黑盒条件，不得把普通图像噪声标为对抗样本。对抗结果应同时报告全部攻击尝试、有效样本率、攻击成功率和质量约束。

### 6.3 时间与空间标注重映射

缩放、裁剪、变帧率、丢帧和音频重采样会改变坐标或时间轴。派生样本必须保存从源样本到输出样本的变换矩阵或时间映射。自动重映射后应抽样人工核验；无法可靠重映射的样本不得用于定位指标。

## 7. G1—G4 泛化分组规范

### 7.1 四象限定义

| 组别 | 方法族 | 作用域 | 评价目的 |
|---|---|---|---|
| G1 | 已知 | 已知 | 基础能力锚点 |
| G2 | 已知 | 未知 | 作用域迁移能力 |
| G3 | 未知 | 已知 | 方法泛化能力 |
| G4 | 未知 | 未知 | 开放组合能力 |

### 7.2 划分步骤

1. 冻结方法族词表和作用域词表；
2. 获取被测对象训练、调参、阈值选择和适配数据声明；
3. 在方法族和作用域类别层级建立可见性矩阵；
4. 将来源组整体分配到 G1—G4；
5. 检查身份、源视频、事件、生成输入和近重复内容是否跨象限；
6. 由独立人员复核“未知”成立条件；
7. 冻结象限清单及其哈希，并在报告中分别给出四象限结果。

G1—G4 可以是同一数据版本中的逻辑视图，但其成员和判定依据必须显式列出。不得通过测试后重新定义方法族或作用域以改善结果。

### 7.3 跨数据集泛化

用于 F-1 的源数据和目标数据应在数据集来源、主体、源媒体和近重复内容上去重。若不同公开数据集共享原始网络素材，应追溯并归并来源组。无法排除共享内容时，应在报告中标记潜在污染，不将其作为高级泛化门槛的唯一证据。

## 8. 标注体系

### 8.1 标注层级

| 层级 | 必需内容 | 适用指标/任务 |
|---|---|---|
| 数据集级 | 数据卡、许可、构成、词表、版本、限制 | 全部 |
| 来源组级 | 源实体、身份/事件、生成批次、近重复关系 | 防泄漏、F类 |
| 样本级 | 真伪、模态、对象、方法、作用域、质量、分区 | T1—T8 |
| 模态级 | 音频、视觉、文本各自真伪和冲突关系 | A类、T2/T8 |
| 片段级 | 起止时间、模态、对象、伪造类型、容差 | C-9、T4 |
| 帧级 | 帧号、时间戳、是否伪造、可见性 | C-8、T4 |
| 区域级 | bbox、polygon、mask或track、对象和置信度 | B/C/G类、T3/T4/T8 |
| 证据级 | 证据类型、位置、说明、来源、可复核状态 | G类、T8 |
| 扰动级 | 基准样本、变换参数、严重度和映射 | E类、T6 |

### 8.2 最小样本清单字段

| 字段 | 类型 | 必要性 | 说明 |
|---|---|---|---|
| `sample_id` | string | 必填 | 数据集内永久唯一，不复用 |
| `group_id` | string | 必填 | 分区原子组 |
| `parent_sample_id` | string/null | 派生样本必填 | 干净或直接父样本 |
| `source_id` | string | 必填 | 源媒体或来源记录 |
| `file_path` | string | 必填 | 相对数据根目录路径 |
| `sha256` | string | 必填 | 文件完整性哈希 |
| `source_type` | enum | 必填 | SRC-PUB/CTL/GEN/EXT/WLD |
| `media_type` | enum | 必填 | video/audio/image/text/multimodal |
| `label_state` | enum | 必填 | REAL/FAKE/UNCERTAIN/OUT-OF-SCOPE |
| `fake_modalities` | array | FAKE 必填 | M1—M5 及细分类 |
| `fake_objects` | array | FAKE 必填 | O1—O4，可多值 |
| `fake_method` | string | 有证据时必填 | 具体方法或工具 |
| `method_family` | string | 泛化试验必填 | 方法族 |
| `scope_id` | string | 泛化试验必填 | 作用域词表项 |
| `granularity` | enum | FAKE 必填 | local/object/scene/full_generation |
| `duration_sec` | number/null | 音视频必填 | 以解码后媒体为准 |
| `width`,`height`,`fps` | number/null | 视频/图像按需必填 | 同时记录可变帧率状态 |
| `video_codec`,`audio_codec` | string/null | 音视频必填 | 无音轨时为 null |
| `partition` | enum | 必填 | D0/D1/D2/D3 |
| `license_status` | enum | 必填 | 权利与使用状态 |
| `access_class` | enum | 必填 | public/internal/restricted/secret |
| `annotation_version` | string | 必填 | 标注版本 |
| `review_status` | enum | 必填 | draft/double_annotated/adjudicated/expert_reviewed |
| `exclusion_status` | enum | 必填 | included/excluded/quarantined |
| `exclusion_reason` | string/null | 排除时必填 | 使用受控词表和补充说明 |

个人姓名、身份证号、联系方式等直接身份信息不应写入通用样本清单。确有必要保存的敏感映射应加密、分库存储并实行最小授权。

### 8.3 时间标注

- 使用秒和整数帧号双重表示，时间基准以解码时间戳为准；
- 片段采用左闭右开区间 `[start, end)`；
- 记录可变帧率、缺帧、音视频起点偏移和时间基信息；
- 多个不连续伪造片段应分别标注，不得用一个大区间吞并真实间隔；
- 边界容差应按任务和媒体帧率在 D1 校准，并在 D2 前冻结；
- 无法确定精确边界时记录不确定区间和置信度，不得伪造精确值。

### 8.4 空间标注

- 优先使用能够表达真实伪造范围的 mask 或 polygon；只具备粗粒度证据时可使用 bbox；
- 坐标应采用像素坐标并声明原点、闭合规则和图像尺寸；
- 跨帧对象应使用稳定 `track_id`；
- 被遮挡或离开画面的状态应显式编码；
- 区分“伪造区域”“受伪造影响的对象区域”和“人工认为可疑的证据区域”；
- 自动生成的掩膜应经过抽样或全量复核，不能因来源于生成流程而免检。

### 8.5 文字与语义标注

M4/O4 样本至少记录语言、文字区域、可见转写、原始文本、修改后文本、修改类型和是否改变核心语义。OCR 自动结果仅为预标注；低清、遮挡、特殊字体或多语言内容应由人工复核。

### 8.6 证据标注

证据标签至少包含：

- `evidence_id`、`sample_id` 和关联片段/区域；
- 证据类型，如视觉伪影、时序不连续、音画冲突、文字冲突、元数据异常、生成记录；
- 人工可观察事实与推断结论，两者分开记录；
- 证据来源、复核人员和复核状态；
- 对应真值和可支持的结论范围；
- 不确定性、反例或替代解释。

证据文本不得仅写“看起来像伪造”。应描述可定位、可复查的异常。模型热力图可以作为被测输出，不应自动成为数据集真值。

### 8.7 JSONL 记录示例

```json
{
  "sample_id": "DFSTD-V-000001",
  "group_id": "GRP-000072",
  "parent_sample_id": null,
  "source_id": "SRC-CTL-000043",
  "file_path": "media/fake/DFSTD-V-000001.mp4",
  "sha256": "<64位小写十六进制SHA-256>",
  "source_type": "SRC-GEN",
  "media_type": "video",
  "label_state": "FAKE",
  "fake_modalities": ["M2"],
  "fake_objects": ["O1"],
  "fake_method": "face_swap_example",
  "method_family": "identity_replacement",
  "scope_id": "face.full.segment",
  "granularity": "object",
  "duration_sec": 12.48,
  "width": 1920,
  "height": 1080,
  "fps": 25.0,
  "partition": "D1",
  "license_status": "standard_internal",
  "access_class": "internal",
  "annotation_version": "1.0.0",
  "review_status": "adjudicated",
  "exclusion_status": "included",
  "exclusion_reason": null
}
```

示例中的占位符和类别名称用于说明字段结构，正式交付应依据冻结词表和 JSON Schema 自动校验。

## 9. 标注组织与工作流

### 9.1 角色分离

| 角色 | 职责 | 不宜兼任 |
|---|---|---|
| 数据管理员 | 来源、许可、版本、访问权限和分区管理 | D3 结果分析人员 |
| 预处理人员 | 解码、转码、抽帧、预标注和技术质检 | 最终仲裁员 |
| 标注员 A/B | 按指南独立完成标签和定位 | 彼此协商后再提交初标 |
| 仲裁员 | 处理冲突，记录裁决依据 | 原冲突双方中的唯一一人 |
| 领域专家 | 复核重要目标、场景和文字语义 | 仅凭模型输出作结论 |
| 质检员 | 抽检、一致性统计、逻辑校验和回退 | 修改标签而不留记录 |
| D3 保管员 | 标签隔离、授权执行和解封审计 | SUT 调参或阈值设计 |

### 9.2 标注流程

1. 冻结标注指南、词表、工具版本和试标样本；
2. 标注员培训并通过资格试标；
3. A/B 两名标注员在不知道对方结果的条件下独立标注；
4. 自动计算标签、时间、区域和证据层面的分歧；
5. 对冲突样本进行书面说明和第三方仲裁；
6. 对重要人物、武器装备、场景语义和文字语义执行专家复核；
7. 运行格式、逻辑、边界、哈希和分区校验；
8. 形成质检报告并冻结标注版本。

### 9.3 冲突规则

以下情况必须进入仲裁：

- REAL/FAKE/UNCERTAIN 不一致；
- M、O、方法族或作用域主类别不一致；
- 时间或空间标注差异超过冻结容差；
- 一方认为证据不足或超出范围；
- 涉及敏感身份、重要目标或核心语义变更；
- 标签与生成日志、源媒体或文件结构存在矛盾。

仲裁不得简单采用多数投票替代证据判断。裁决记录应保存原始标注、分歧点、采用结论、依据、责任人和时间。

### 9.4 标注一致性

一致性指标按数据类型选择：

- 名义分类：Cohen's kappa、Fleiss' kappa 或 Krippendorff's alpha；
- 有序等级：加权 kappa；
- 连续时间边界：绝对误差、区间 IoU 和组内相关；
- 空间区域：IoU、Dice 和边界距离；
- 多标签：逐标签一致率、micro/macro F1 和集合相似度；
- 专家量表：ICC 或加权一致性指标。

具体通过阈值应在试标阶段结合任务风险和偶然一致率校准。仅报告总体一致率不足以验收，应同时报告各子组、争议率、仲裁率和低一致性原因。

## 10. 自动质检与人工质检

### 10.1 文件级检查

每个媒体文件至少检查：

- 路径存在、文件大小合理且 SHA-256 与清单一致；
- 可完整解码，无非预期截断、黑帧、静音或轨道缺失；
- 容器、编码、时长、分辨率、帧率和音频属性与清单一致；
- 不含未授权附加文件、脚本或可执行内容；
- 敏感元数据已按策略保留、脱敏或移除。

### 10.2 标注逻辑检查

自动校验至少包括：

- REAL 样本不得出现未解释的伪造片段或区域；
- FAKE 样本必须具备伪造模态、对象、方法/未知状态和作用域；
- 时间区间处于媒体时长内，且 `start < end`；
- 空间坐标处于对应帧尺寸内；
- mask、polygon、bbox 和 track 引用的帧存在；
- 派生样本的父样本存在，且真值继承与变换记录一致；
- `group_id` 不跨 D0—D3；
- D3 不与任何训练、调参、D0—D2 清单共享禁止实体；
- 排除样本不会进入正式运行清单；
- 词表值、Schema 版本和必填条件全部通过。

### 10.3 去重检查

去重应组合使用：

- 加密哈希发现字节级重复；
- 图像/关键帧感知哈希发现缩放、压缩和轻微编辑后的近重复；
- 音频指纹发现重编码和片段复用；
- 视频时序指纹或嵌入相似度发现裁剪、变速和片段重叠；
- OCR 文本和元数据用于辅助发现模板或内容复用；
- 高风险候选对由人工复核。

相似度阈值应在 D0/D1 上校准，并保存候选对、阈值、算法版本和人工结论。不得仅依靠文件名或 SHA-256 宣称无泄漏。

### 10.4 内容质量抽检

人工抽检应覆盖全部分区、来源、M/O/R/G 组和标注人员。以下样本应提高抽检或全检比例：

- D2/D3 样本；
- G4、重要目标、M4/M5、O2—O4；
- 自动生成掩膜和自动时间映射；
- 低一致性或经仲裁样本；
- 来源、许可或真值证据较弱的样本；
- 阈值附近和历史上高错误率的样本。

抽检比例和接受准则应在数据建设计划中预注册，不能在看到结果后降低要求。

## 11. 样本规模与平衡

### 11.1 独立单位

分类指标的样本量应按独立源视频或更高层级来源组计算，不按帧数、预测框数或扰动副本数计算。定位任务按“至少含一个可评估实例的独立源视频数”规划。

### 11.2 精度驱动的规模设计

比例指标可按目标置信区间半宽估算初始独立样本量：

```text
n ≈ z² × p × (1-p) / h²
n_adjusted = n × DEFF
```

其中 `h` 为目标半宽，`DEFF` 为身份、来源和方法聚类导致的设计效应。实际样本量应由 D1 的先导结果、阳性率、子组数量、缺失率和边界稳定性共同确定。

### 11.3 覆盖矩阵

数据验收应至少输出以下交叉表，并以独立来源组计数：

1. REAL/FAKE × D0—D3；
2. M1—M5 × O1—O4；
3. M/O × 方法族；
4. M/O × 作用域；
5. R1—R8 × 严重度；
6. G1—G4 × REAL/FAKE；
7. 来源类型 × 场景 × 质量层；
8. 重要目标/非重要目标 × 复核状态。

“总体数量充足”不能替代子组覆盖。若某个门槛项的最弱子组不足，应补充数据、合并有理论依据的类别、降低为扩展项或扩大不确定区，不得只依赖加权平均。

### 11.4 平衡策略

标准数据集不要求所有交叉单元机械等量，但应避免单一来源、身份、方法或场景主导结果。抽样、重加权或分层汇总策略须预注册，并同时公布未加权原始计数与分组结果。

## 12. 安全、许可、隐私与伦理

### 12.1 来源与许可

每个来源应记录权利人、获取方式、许可文本或协议版本、允许用途、再分发条件、地域限制、期限和撤回机制。许可状态不明确的样本不得进入对外发布包；进入内部验证包也应经过书面风险审查。

### 12.2 个人信息与肖像声音

涉及自然人肖像、声音、身份或行为的数据，应遵循合法、正当、必要和最小化原则。能够使用合成、授权或脱敏数据完成的测试，不应额外收集真实敏感信息。身份映射、联系方式和授权文件应与测试清单分离存储。

### 12.3 重要目标和敏感内容

涉及重要人物、武器装备、敏感地点、未成年人、暴力或其他受限内容时，应设置访问级别、审批流程、水印、下载审计和用途限制。公开摘要不得泄露受限媒体、详细位置或可能促进滥用的生成参数。

### 12.4 双重用途控制

生成工具、模型权重、攻击脚本和高保真伪造素材不应因评测需要而默认随数据发布。正式交付应把“运行评测所需”与“复现伪造生产过程所需”分开授权，并记录导出和复制行为。

### 12.5 删除与撤回

样本因授权撤回、权利争议、泄漏或严重标注错误需要删除时，应：

1. 隔离该样本及其全部派生版本；
2. 查找受影响来源组、分区、实验和报告；
3. 发布新数据版本和变更说明；
4. 判断既有校准或确认结论是否需要重跑；
5. 保留不可公开的审计记录，不保留已无权保存的媒体副本。

## 13. 版本与变更管理

### 13.1 版本对象

以下对象分别版本化并在发布清单中绑定：

- 媒体数据版本；
- 样本清单 Schema 版本；
- 标注词表和指南版本；
- 标注结果版本；
- 分区与 G1—G4 视图版本；
- 扰动生成配置版本；
- 质检规则与去重算法版本；
- 许可和访问策略版本。

### 13.2 版本升级

可采用 `主版本.次版本.修订版本`：

- 主版本：分区、核心标签含义或大批样本改变，既有评测不可直接比较；
- 次版本：新增样本、类别或可选标注，保持主要口径兼容；
- 修订版本：修复不改变总体设计的小量标签、路径或元数据错误。

D2/D3 使用的版本一经预注册应只读冻结。任何会影响指标的修订都应生成新版本并评估是否重跑。

### 13.3 变更记录

变更日志至少包含变更编号、日期、申请人、审核人、原因、受影响样本、旧值、新值、影响评估、是否重跑、关联报告和新版本号。

## 14. 数据包交付结构

```text
dataset_root/
├─ README.md
├─ DATASET_CARD.md
├─ CHANGELOG.md
├─ LICENSES/
│  ├─ usage-policy.md
│  └─ source-license-index.csv
├─ schemas/
│  ├─ sample-manifest.schema.json
│  ├─ annotation.schema.json
│  └─ vocabularies.json
├─ manifests/
│  ├─ samples.jsonl
│  ├─ sources.jsonl
│  ├─ groups.jsonl
│  ├─ splits.jsonl
│  └─ exclusions.jsonl
├─ media/
│  ├─ real/
│  └─ fake/
├─ annotations/
│  ├─ sample/
│  ├─ temporal/
│  ├─ spatial/
│  ├─ text/
│  └─ evidence/
├─ perturbations/
│  ├─ configs/
│  └─ mappings/
├─ quality/
│  ├─ validation-report.json
│  ├─ agreement-report.md
│  ├─ dedup-report.md
│  └─ coverage-tables/
├─ checksums/
│  └─ SHA256SUMS
└─ docs/
   ├─ annotation-guideline.md
   ├─ adjudication-guideline.md
   ├─ access-control.md
   └─ known-limitations.md
```

D3 包应将媒体运行包与标签保管包分离。对外发布版本可以只交付获准内容，但删减项、删减原则和对评价范围的影响必须写入数据卡。

## 15. 数据卡最低内容

`DATASET_CARD.md` 至少应说明：

1. 建设目的、适用任务和禁止用途；
2. 数据来源、采集/生成流程和时间范围；
3. REAL/FAKE、M/O/R/G、方法族、作用域和场景分布；
4. 独立来源组数量及样本量计算单位；
5. 标注流程、人员构成、一致性和仲裁结果；
6. 去重方法、防泄漏规则和已知污染风险；
7. 许可、隐私、安全、访问和撤回政策；
8. D0—D3 分区方法和 D3 保管机制；
9. 已知限制、缺失子组和不应外推的范围；
10. 版本、哈希、Schema、词表和变更记录；
11. 联系、问题报告和勘误流程。

## 16. 数据验收门

| 门 | 通过条件 | 失败后的动作 |
|---|---|---|
| DG0 范围冻结 | M/O/R/G、方法族、作用域和用途定义无歧义 | 修订词表和标注指南 |
| DG1 权利安全通过 | 每个样本许可、隐私和访问级别明确 | 隔离或删除问题样本 |
| DG2 技术质量通过 | 文件可解码、元数据一致、哈希完整 | 修复或排除样本 |
| DG3 标注质量通过 | 双人标注、仲裁、专家复核和一致性满足预注册要求 | 返工、培训或修改指南 |
| DG4 防泄漏通过 | 来源组原子划分，跨分区重复与禁止关联为零 | 重新归组和划分 |
| DG5 覆盖与规模通过 | 关键子组具有足够独立单位和可接受不确定性 | 补样或调整指标角色 |
| DG6 D2 冻结 | 清单、标签、分区、Schema和哈希不可变 | 不进入阈值校准 |
| DG7 D3 托管 | 标签隔离、授权、一次性运行和解封规则就绪 | 不执行独立确认 |

任何一项门槛未通过时，数据包不得被描述为“标准验证数据集正式版”。可按实际状态标为开发版、探索版或受限试用版。

## 17. 与 E0—E12 实验的接口

| 实验 | 数据接口要求 |
|---|---|
| E0 计算链路单元验证 | D0 合成完美、相反、随机、恒定、缺失和边界真值 |
| E1 可计算性冒烟 | 覆盖基础输入输出、缺失轨道、异常文件和最小 M/O 子集 |
| E2 重复性 | 固定媒体、标签、运行顺序和结果哈希 |
| E3 基础区分度 | REAL/FAKE、主要 M/O/方法族分层且来源不过度集中 |
| E4 样本量稳定性 | 可按来源组、身份、方法和子组重采样的层级清单 |
| E5 鲁棒性强度 | 干净—轻—中—重配对链和完整变换参数 |
| E6 四象限泛化 | 方法族、作用域、可见性矩阵和 G1—G4 冻结清单 |
| E7 取证盲评 | 匿名媒体、区域/片段/证据真值和专家评分材料 |
| E8 归一化消融 | 原始指标所需全部真值和固定数据版本 |
| E9 权重消融 | 同 E8，另保留场景和风险子组 |
| E10 门槛上限消融 | 门槛项关键子组和失败样本可追溯 |
| E11 候选阈值校准 | D2 冻结包、预注册排除规则和独立单位清单 |
| E12 独立确认 | D3 隐藏标签包、一次性执行清单和独立确认报告模板 |

## 18. 与 T0—T9 和能力指标的接口

| 数据能力 | 主要任务 | 主要指标类别 |
|---|---|---|
| 样本和版本清单 | T0 | A-1、B-1、H类声明核查 |
| REAL/FAKE 与连续分数真值 | T1 | C-1—C-7 |
| M1—M5 | T2 | A-2、A-3 |
| O1—O4 | T3 | B-2、B-3 |
| 时间、帧和空间定位 | T4 | C-8—C-10、G-2 |
| 时长、分辨率、编码和工作负载 | T5 | D-1—D-5 |
| R1—R8 配对样本 | T6 | E-1—E-4 |
| 方法族、作用域和 G1—G4 | T7 | F-1—F-4 |
| 证据、报告和专家真值 | T8 | G-1—G-5 |
| 安装任务、异常样本和操作材料 | T9 | H-1—H-5 |

## 19. 异常与边界情形

### 19.1 无音频或多音轨

无音频应显式记录 `audio_present=false`，不能当作处理失败。多音轨应标明默认轨、语言、各轨真值和是否参与测试。

### 19.2 可变帧率与时间戳异常

可变帧率样本不得仅使用 `帧号/fps` 推导时间。应保存解码时间戳；时间戳不单调或无法修复的样本不得用于精确时间定位。

### 19.3 多重和重叠伪造

多种伪造方法、模态或对象重叠时，应保留多个实例和层级关系。主标签只用于分层，不得丢弃次要真值。

### 19.4 真值不确定

来源声明与人工证据冲突、方法不可确认或边界高度模糊的样本进入 `UNCERTAIN` 或隔离区。不得为满足样本量而强行二分类。

### 19.5 文件损坏与处理失败

用于测试异常处理能力的损坏样本应与正常识别样本分开编码，并声明预期状态。非计划损坏的样本应修复、替换或排除，不能把 SUT 的正确报错计为识别失败。

### 19.6 凭证与溯源元数据

若媒体包含内容凭证、签名或来源元数据，应记录其存在性和验证状态。评测应明确 SUT 是否允许使用此类信息，并设置有/无凭证的对照，避免把凭证验证能力与内容检测能力混为一项。

## 20. 交付前检查清单

### 20.1 范围和构成

- [ ] REAL/FAKE、M1—M5、O1—O4、R1—R8、G1—G4 均有清单和覆盖说明；
- [ ] 方法族和作用域词表已冻结；
- [ ] 关键稀缺组和不覆盖范围已在数据卡中说明；
- [ ] 样本量按独立来源组而非帧数统计。

### 20.2 标注和质量

- [ ] 主标签经过双人独立标注；
- [ ] 冲突、低置信和敏感样本完成仲裁或专家复核；
- [ ] 时间、空间、文本和证据标注通过逻辑校验；
- [ ] 一致性、仲裁率、缺失率和排除率已报告；
- [ ] 全部文件可解码，清单和 SHA-256 一致。

### 20.3 防泄漏和分区

- [ ] `group_id` 原子分区检查通过；
- [ ] 完成字节级、感知、音频和时序近重复检查；
- [ ] 训练、调参、D0、D1、D2、D3 的禁止关联检查通过；
- [ ] G3/G4 在方法族层级成立，G2/G4 在作用域类别层级成立；
- [ ] D3 标签、权限和解封记录由独立人员管理。

### 20.4 合规和版本

- [ ] 来源、许可、个人信息、敏感内容和使用边界明确；
- [ ] 数据、Schema、词表、标注、分区和质检规则均有版本；
- [ ] 变更日志、数据卡、已知限制和撤回机制完备；
- [ ] 数据包结构、Schema 校验和哈希复核通过。

## 21. 待实验校准与专家确认事项

以下内容不得在没有验证证据时直接固化为强制阈值：

1. R1—R8 轻、中、重的具体参数及感知质量下限；
2. 时间边界、空间边界和 OCR 转写的允许容差；
3. 不同标签类型的一致性验收阈值；
4. 各 M/O/G 子组和重要目标的最低独立样本量；
5. 感知哈希、视频指纹和嵌入相似度的近重复判定阈值；
6. 自动预标注、自动掩膜和自动重映射的人工抽检比例；
7. “已知/未知作用域”的正式分类表和跨类别距离；
8. D3 失败后新保留集的重建规模和冷却规则；
9. 对外发布包与内部受限包的字段和媒体删减边界。

上述事项应在 D1 形成候选方案，在 D2 按预注册规则冻结，并由领域专家、数据治理人员和评测人员共同确认。

## 22. 阶段输出

执行本规范应形成：

1. 数据建设方案和来源登记表；
2. 方法族、作用域、模态、对象、扰动和排除原因词表；
3. 标注指南、试标集和标注员资格记录；
4. 样本、来源组、分区、排除和许可清单；
5. 时间、空间、文字、证据和扰动标注；
6. 一致性、仲裁、专家复核和质检报告；
7. 去重、防泄漏和 G1—G4 划分报告；
8. 覆盖矩阵、样本量和不确定性报告；
9. 数据卡、Schema、哈希、版本和变更日志；
10. D3 独立保管、授权执行和解封审计材料。

## 23. 下一步工作

在本规范通过项目组审查后，建议编制：

> `10-最小可行实验实施方案.md`

该文件应选取可合法获取的公开参考数据和小规模受控样本，建立首批 Schema、清单和验证脚本，执行 E0—E4 的最小闭环，并为 E5 扰动强度和 E6 泛化划分提供先导数据。最小实验只能验证流程和指标可计算性，不得提前发布正式等级阈值。
