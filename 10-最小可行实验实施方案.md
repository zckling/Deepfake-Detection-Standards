# 深度伪造识别标准最小可行实验实施方案

> 文件性质：E0—E4 最小实验闭环的实施与验收方案。
>
> 上游依据：`07-能力体系与指标体系汇总报告.md`、`08-指标计算方法与等级阈值校准方案.md`、`09-标准验证数据集需求与标注规范.md`。
>
> 版本状态：v0.1，供首轮实验环境、数据、Schema、被测对象适配器和指标链路建设使用。
>
> 证据边界：本方案只验证流程、计算正确性、可计算性、初步重复性、基础区分度和样本量稳定趋势，不用于发布正式 L1—L4 能力等级阈值。

## 1. 实施目标

本方案建立一个能够真实运行、产生结构化结果并支持审计的最小实验闭环。闭环应从数据清单开始，经被测对象运行、统一输出、指标计算、统计分析和结果归档，最终回答以下问题：

1. 39 项指标的输入、输出和适用性规则能否被程序化表达；
2. 关键分类、定位、效率、鲁棒性、泛化性、取证和工程指标能否沿统一计算链路得到结果；
3. 同一被测对象在冻结条件下重复运行，结果是否可复现或能解释其随机波动；
4. 指标是否能够区分合成边界基线、能力缺项对照和不同形态的真实被测对象；
5. 当独立样本数逐步增加时，指标区间、对象排序和结论是否趋于稳定；
6. 现有标准草案中哪些定义、字段或统计规则仍不能执行，需要回写修订。

最小实验的交付结果不是一个更强的检测模型，而是一条可验证的评价流水线和一组能够否定现有设计假设的实验结果。

## 2. 完成定义

本轮工作只有在以下条件同时满足时才视为“最小闭环完成”：

- 已建立样本、来源组、分区、标注、被测对象输出、运行记录和指标结果的机器可读 Schema；
- 已生成可人工复算的 E0 合成真值和预测用例；
- 至少三类能力形态不同的被测对象或适配对象完成 D0 冒烟；
- E0—E4 均具有冻结配置、原始结果、预先设计的表格或图和结论记录；
- 全部排除、失败、超时和不可计算项均有结构化原因，不得静默删除；
- 同一实验能够由另一名执行人员根据运行清单复现；
- 形成问题清单，明确回写指标字典、数据规范或实验协议的位置；
- 明确声明本轮不能支持正式能力等级阈值。

若某项因数据许可、算力或被测对象能力缺失不能完成，应标为“阻塞”或“超出首轮范围”，不能以空值或主观说明冒充验证结果。

## 3. 实验范围

### 3.1 本轮纳入范围

| 类别 | 纳入内容 | 目的 |
|---|---|---|
| E0 | 合成边界基线、人工复算和自动回归 | 证明指标实现方向、边界和异常处理正确 |
| E1 | 小规模 D0 冒烟和多能力形态适配 | 证明指标具有可执行路径 |
| E2 | 冻结条件下的重复运行 | 识别确定性、随机性和环境漂移 |
| E3 | 同一数据上的多被测对象配对比较 | 初步评价指标区分度和天花板/地板效应 |
| E4 | 逐级增加独立来源组的重采样 | 观察区间宽度、排序和结论稳定性 |
| 数据 | D0 与 D1 的受控子集 | 支撑流程和探索性实验 |
| 能力 | 以 C、D 为主，兼顾 A、E、F、G、H 的计算入口 | 验证主链路与适用性，而非完成全部等级校准 |

### 3.2 本轮不纳入范围

- D2 正式归一化、权重、门槛和等级切点校准；
- D3 独立保留集确认；
- E5—E12 的正式结论；
- 对 R1—R8 轻、中、重参数作最终冻结；
- 对 G1—G4 高级泛化能力作正式等级判定；
- 宣称覆盖所有伪造方法、对象、场景、语言或传播链路；
- 以本轮参测模型的相对排名直接定义 L1—L4；
- 为获得完整指标而虚构被测对象不具备的输出能力。

## 4. 待检验主张与否证条件

### 4.1 主张矩阵

| 编号 | 主张 | 对应实验 | 最小支持证据 | 否证条件 |
|---|---|---|---|---|
| MVP-H0 | 指标计算链路在典型和边界输入上实现正确 | E0 | 自动结果与人工预期逐项一致 | 任一核心指标方向、分母、缺失或边界处理错误 |
| MVP-H1 | 39 项指标均具有明确的适用性判断和可执行入口 | E1 | 每项得到数值、布尔/清单结果或合法不适用原因 | 关键指标输入定义不清，或无法区分“不支持”和“计算失败” |
| MVP-H2 | 冻结条件下的结果可重复，非确定性波动可以量化 | E2 | 结果哈希一致，或多次运行波动有记录和解释 | 同一条件产生无法解释的大幅差异或版本漂移 |
| MVP-H3 | 至少部分主指标能够区分能力形态不同的对象 | E3 | 配对差值方向合理、区间可报告、失败模式可解释 | 多数主指标恒定、饱和、反向或被数据捷径主导 |
| MVP-H4 | 增加独立来源组能缩小不确定性并减少排序波动 | E4 | 区间宽度和排序稳定曲线趋于收敛 | 样本增加后仍无趋势，或结论被少数来源组持续支配 |

### 4.2 证据使用规则

一次实验只应回答一个主问题。探索中发现的新问题应登记为后续实验，不得在同一运行中不断改变数据、模型、阈值和统计方法，再挑选有利结果。

本轮对 MVP-H3、MVP-H4 只形成探索性结论。未经 D2 预注册和 D3 独立确认，不得将其升级为正式标准阈值证据。

## 5. 数据与许可方案

### 5.1 候选来源

| 来源 | 本轮角色 | 可支持内容 | 许可/访问状态 | 首轮决策 |
|---|---|---|---|---|
| 本地合成真值与预测 | E0 主数据 | 完美、相反、随机、恒定、缺失、定位边界 | 项目自行生成，不含外部媒体 | 必选 |
| DeepfakeBench | 视觉模型运行和统一评测参考框架 | 视觉检测、跨数据集接口、候选预训练模型 | 仓库为 CC BY-NC 4.0；其聚合数据仍受原始提供方条件约束 | 必选框架候选，不自动继承数据授权 |
| DF40 | D0/D1 视觉伪造候选子集 | M2、O1、多方法族、跨方法探索 | 官方仓库声明数据与代码为 CC BY-NC 4.0；获取前仍须核验来源数据条件 | 许可审查通过后纳入 |
| LAV-DF | D0/D1 音视频与时间定位候选子集 | M1、M2、M5、时间片段和 BA-TFD 基线 | 使用前须同意条款；项目代码声明 CC BY-NC 4.0 | 许可审查通过后纳入 |
| AV-Deepfake1M | 后续扩展候选 | 大规模音频、视觉和联合伪造及片段定位 | 数据访问需要签署 EULA；代码许可分项不同 | 不作为首轮硬依赖 |
| 受控自建小样本 | 字段、流程和缺口验证 | O2—O4、M4、异常样本和边界案例 | 需单独完成授权、安全和标注流程 | 少量加入 D0，不作正式性能结论 |

官方资料核验日期为 2026-09-23。下载或复制数据前必须重新检查官方仓库、许可文件、条款和访问表单；本表不能替代法律或数据治理审核。

### 5.2 最小数据组合

首轮建议建立三个逻辑包：

| 数据包 | 目标规模 | 构成 | 用途 |
|---|---:|---|---|
| `D0-FIXTURE` | 由测试用例决定，不追求统计规模 | 人工构造真值、预测和异常输出 | E0 |
| `D0-SMOKE` | 约 120 个独立输入，具体以合法可得性为准 | REAL/FAKE 基本平衡，覆盖视觉和音视频基础形态 | E1 |
| `D1-PILOT` | 目标 400—600 个独立来源组 | REAL/FAKE 分层，至少覆盖两个数据来源和多个方法族 | E2—E4 |

这些数量是工程实施预算，不是标准最低样本量。D1-PILOT 的主要用途是观察稳定曲线并估计设计效应；任何关键子组样本不足都应单独报告，不得用总体数量掩盖。

### 5.3 抽样规则

1. 以 `group_id` 为抽样和分区原子，不随机拆分帧；
2. 同一源视频、身份、事件、生成批次和近重复内容只能进入一个逻辑包；
3. 同一源媒体的原始、伪造和扰动版本整体归组；
4. D0 与 D1 不共享禁止实体；
5. 在每个规模档位保持预注册的 REAL/FAKE、来源和方法族分层；
6. 优先选择标签、时间边界和许可记录可审计的样本；
7. 对无法确认来源组的公开样本，先隔离，不直接投入 E3/E4；
8. 保存全部候选样本、纳入样本和排除样本清单及原因。

### 5.4 许可先行门

外部数据进入项目目录前应完成：

- 官方下载页和许可文件归档；
- 数据用途与“研究、标准验证、内部共享、对外发布”的兼容性审查；
- 是否允许派生文件、抽帧、缓存、模型评测和结果公开的核验；
- 是否包含第三方来源或需要额外授权的识别；
- 访问人员、存储路径、复制限制和删除机制登记；
- 对外结果仅发布统计量、样本编号或获准示例，不默认发布媒体。

许可门未通过时，可以继续 E0 和 Schema 开发，但不得下载、复制或纳入相应媒体。

## 6. 被测对象与基线

### 6.1 被测对象组合

| 编号 | 能力形态 | 候选实现 | 主要用途 |
|---|---|---|---|
| SUT-V1 | 视频/帧级视觉检测器 | DeepfakeBench 中可复现的 Xception 或等价基础检测器 | C 类准确度、E2—E4 主对象 |
| SUT-V2 | 与 V1 架构或训练策略不同的视觉检测器 | DeepfakeBench 中第二种可运行检测器 | 区分度和排序对照 |
| SUT-AV1 | 音视频检测与时间定位 | LAV-DF 官方 BA-TFD/BA-TFD+ 中资源可承受者 | A 类、多模态、C-9 和输出适配 |
| SUT-W1 | 工程包装原型 | 在一个检测器外增加统一接口、批处理、日志和报告 | G/H 类计算入口 |
| SUT-C1 | 能力缺项对照 | 仅保留视频级标签与分数，主动移除定位和证据字段 | 验证适用性、缺项和等级上限规则 |

具体模型只有在“代码可获得、权重可合法使用、环境可复现、输入输出可转换”四项均满足后才能登记为正式参测对象。若某个候选不能稳定运行，应替换为相同能力形态的对象，而不是修改实验主张。

### 6.2 计算链路基线

| 基线 | 生成规则 | 应验证的边界 |
|---|---|---|
| B-PERFECT | 预测与真值完全一致 | 指标上界、定位完全重合、无失败 |
| B-INVERSE | 标签或分数方向完全反转 | 正类方向、指标下界、EER 边界 |
| B-RANDOM | 固定随机种子生成标签和分数 | 机会水平、重复性、随机种子记录 |
| B-CONSTANT | 所有样本同一标签或同一分数 | 并列分数、无方差和不可定义指标 |
| B-MAJORITY | 全部预测多数类 | 类别不平衡下 ACC 虚高 |
| B-MISSING | 按固定模式制造字段缺失、错误和超时 | 失败计入、不可计算和输出完整性 |
| B-NO-EVIDENCE | 只输出真伪与分数 | G 类缺项和能力上限入口 |

合成基线只用于验证评价系统，不作为真实模型能力水平的证据。

### 6.3 被测对象冻结信息

每个 `system_id` 至少登记：

- 名称、代码仓库、提交哈希和许可证；
- 权重来源、权重哈希和训练数据声明；
- 配置文件、阈值、随机种子和预处理；
- Python、框架、CUDA/cuDNN、驱动和依赖锁文件；
- CPU、GPU、内存、存储和操作系统；
- 支持的 M/O 组、输出粒度和已知限制；
- 适配器版本、输入输出 Schema 版本和错误码映射；
- 首次成功运行的样例和结果哈希。

## 7. 统一数据和结果接口

### 7.1 首批 Schema

| Schema | 文件建议 | 关键内容 |
|---|---|---|
| 样本清单 | `schemas/sample-manifest.schema.json` | 09 文件规定的样本、来源组、真值、分区和许可字段 |
| 时间/空间标注 | `schemas/annotation.schema.json` | 片段、帧、bbox、polygon、mask、track 和证据 |
| 被测对象登记 | `schemas/system-registry.schema.json` | 版本、权重、能力声明、环境和适用任务 |
| 统一预测输出 | `schemas/prediction.schema.json` | 标签、连续分数、模态、对象、片段、区域、证据和状态 |
| 运行记录 | `schemas/run-manifest.schema.json` | 数据、SUT、配置、环境、开始结束时间和哈希 |
| 指标结果 | `schemas/metric-result.schema.json` | 指标值、分母、适用性、区间、失败和版本 |
| 排除记录 | `schemas/exclusion.schema.json` | 样本、阶段、原因、申请人、审核人和影响 |

### 7.2 统一预测最低字段

```json
{
  "sample_id": "DFSTD-V-000001",
  "system_id": "SUT-V1",
  "system_version": "<frozen-version>",
  "run_id": "RUN-E1-V1-001",
  "is_fake_pred": true,
  "fake_score": 0.82,
  "processing_time_sec": 0.153,
  "status": "ok",
  "error_code": null,
  "schema_version": "1.0.0"
}
```

增强能力字段只在 SUT 声明支持时提供，但必须通过适用性矩阵区分“条件不适用”“系统不支持”“输出缺失”和“运行失败”。计算程序不得为缺失字段推定默认值。

### 7.3 结果状态枚举

| 状态 | 含义 | 指标处理 |
|---|---|---|
| `ok` | 合法输入且输出完整 | 正常计算 |
| `unsupported` | SUT 明确不支持该能力 | 按适用性规则记录能力缺项 |
| `invalid_input` | 输入不符合已冻结接口 | 数据或调度问题，不直接归责 SUT |
| `runtime_error` | 合法输入导致程序错误 | 分类任务按失败处理并计入任务失败率 |
| `timeout` | 超过预注册时限 | 按超时失败处理 |
| `missing_output` | 运行结束但缺少必填输出 | 相关指标不可计算并触发完整性问题 |
| `not_applicable` | 指标对该 SUT 或样本不适用 | 不进入相应分母并保留依据 |

## 8. 环境与可复现性

### 8.1 环境分层

| 环境 | 目的 | 要求 |
|---|---|---|
| DEV | Schema、单元测试和小样本调试 | 可使用 CPU 或单 GPU，不形成正式效率数据 |
| PILOT | E1—E4 正式首轮运行 | 固定硬件、容器/环境锁、禁止自动更新 |
| REVIEW | 复算和报告检查 | 只读结果包，可独立运行指标脚本 |

若某官方实现依赖旧版 Python 或框架，应使用独立容器或环境，不在同一环境内强行解决互相冲突的依赖。环境差异必须通过适配器隔离，不能改变统一输出语义。

### 8.2 固定项

正式运行期间固定：

- 数据版本、样本顺序和文件哈希；
- 被测对象代码、权重、配置、阈值和预处理；
- 运行硬件、驱动、依赖和并发设置；
- 输入输出 Schema 与适配器版本；
- 指标脚本、正类定义、分母和异常处理；
- 随机种子、确定性设置和重复次数；
- 日志、计时边界和资源采样间隔。

### 8.3 运行前自检

- 所有媒体与清单哈希一致；
- Schema 和词表校验通过；
- 被测对象样例输入能产生可解析输出；
- GPU/CPU、内存和磁盘空间满足配置；
- 系统时间、时区和运行编号规则一致；
- 输出目录为空或为新的不可覆盖目录；
- E0 回归测试通过后才允许进入 E1；
- 原始输出、标准化输出和指标结果分别保存。

## 9. E0 计算链路单元验证

### 9.1 实验问题

在不依赖真实检测模型的条件下，指标脚本是否能够对已知预期的输入给出正确结果，并正确处理并列、空集、缺失、失败和边界情况？

### 9.2 控制设计

| 项目 | 设计 |
|---|---|
| 基线 | B-PERFECT、B-INVERSE、B-RANDOM、B-CONSTANT、B-MAJORITY、B-MISSING |
| 单一变化因素 | 预测输出模式 |
| 固定变量 | 同一真值、同一指标脚本、同一 Schema 和正类定义 |
| 性质 | 开发性单元验证，但结果作为后续实验硬门 |
| 结果载体 | 预期值对照表、单元测试报告、覆盖率和失败日志 |
| 否证条件 | 任一核心指标与人工复算或冻结参考实现不一致 |

### 9.3 用例组

1. 二分类：全对、全错、单类真值、类别不平衡、并列分数；
2. 排序指标：严格排序、反向排序、相同分数、缺少正类或负类；
3. 阈值指标：阈值恰等于分数、阈值两侧、空预测；
4. 时间定位：完全重合、部分重合、包含、分离、零长度和越界；
5. 空间定位：完全重合、偏移、嵌套、空区域、越界区域；
6. 效率：正常、超时、负值、缺失、批处理和预热边界；
7. 失败处理：错误、超时、缺字段、不适用和数据争议；
8. 综合计算：能力缺项、门槛触发、不可计算指标和边界不确定状态。

### 9.4 E0 接受条件

- 预期有定义的核心结果全部与人工复算一致；
- 浮点计算采用预先规定的数值容差；
- 预期无定义的指标返回结构化状态，不产生误导性数值；
- 每个已知边界规则至少有一个正向和一个反向用例；
- 任一失败均阻止进入 E1，修复后重跑全部回归用例。

## 10. E1 可计算性冒烟

### 10.1 实验问题

在真实媒体和不同能力形态的 SUT 上，39 项指标是否都能落入“成功计算、可测布尔/清单结果、明确不适用或能力缺项”之一，而不是因接口歧义而失败？

### 10.2 控制设计

| 项目 | 设计 |
|---|---|
| 对照 | SUT-C1 能力缺项对象和 B-NO-EVIDENCE |
| 单一变化因素 | SUT 能力形态 |
| 固定变量 | D0-SMOKE、运行环境、Schema、适用性矩阵和指标脚本 |
| 性质 | 开发性冒烟 |
| 结果载体 | 指标可计算性矩阵、接口失败表和字段缺口表 |
| 否证条件 | 核心门槛指标普遍无法计算，或缺项与错误无法区分 |

### 10.3 运行矩阵

| 运行 | SUT | 数据 | 主要检查 |
|---|---|---|---|
| E1-R1 | SUT-V1 | D0-SMOKE 视觉子集 | 视频级标签、分数、耗时和错误 |
| E1-R2 | SUT-V2 | 同一视觉子集 | 统一适配和模型间可比性 |
| E1-R3 | SUT-AV1 | 音视频子集 | 模态、片段和时间定位输出 |
| E1-R4 | SUT-W1 | 全部可支持子集 | 批处理、日志、报告和异常处理 |
| E1-R5 | SUT-C1 | 与来源 SUT 相同子集 | 能力缺项、不适用和等级上限入口 |
| E1-R6 | B-MISSING | 合成与小规模真实样本 | 缺失、超时和错误处理 |

### 10.4 E1 接受条件

- 39 项指标全部具有程序化路由和适用性结果；
- 每个适用核心指标能够计算，或生成可定位的缺陷编号；
- `unsupported`、`not_applicable`、`missing_output` 和 `runtime_error` 不混用；
- 原始输出到统一输出的转换不丢失关键字段；
- 任何计算失败均能追溯到样本、SUT、适配器、指标和日志；
- E1 只证明可执行，不证明指标有效或 SUT 达标。

## 11. E2 重复性实验

### 11.1 实验问题

冻结数据、模型、环境、参数和阈值后，同一对象的预测、指标和运行元数据是否稳定？非确定性对象的波动是否能够通过重复运行量化？

### 11.2 控制设计

| 项目 | 设计 |
|---|---|
| 基线 | 每个 SUT 的首次冻结运行 |
| 单一变化因素 | 重复运行编号；随机对象按预注册种子变化 |
| 固定变量 | D1 子集、硬件、环境、模型、阈值、并发、适配器和指标脚本 |
| 性质 | 探索性重复性评价 |
| 结果载体 | 输出哈希表、指标差异表、变异系数和运行间极差 |
| 否证条件 | 确定性对象输出不一致，或非确定性波动无法解释 |

### 11.3 重复规则

- 声明确保确定性的 SUT：正式运行 1 次，再以相同输入复核输出哈希；
- 存在随机采样或非确定推理的 SUT：至少运行 3 次；
- 效率记录：预热后至少重复 5 次，报告中位数、P95、均值和标准差；
- 运行顺序使用冻结清单，必要时采用区组或轮换顺序降低温度和缓存偏差；
- 任何版本、依赖或配置变化都生成新 `system_version`，不能计作同条件重复。

### 11.4 E2 接受与回退

本轮不预设通用变异系数阈值。确定性输出哈希不一致必须停止并定位原因；非确定性结果应报告每次结果和区间。若主指标波动足以改变 E3 的排序或结论，应增加重复数、固定随机源或把该指标标为不稳定候选。

## 12. E3 基础区分度实验

### 12.1 实验问题

在同一 D1 数据和同一阈值策略下，主指标是否能够区分合成上/下界、能力缺项对象和能力形态不同的真实 SUT？

### 12.2 控制设计

| 项目 | 设计 |
|---|---|
| 基线 | B-MAJORITY、B-RANDOM、B-PERFECT 和 SUT-V1 |
| 单一变化因素 | SUT |
| 固定变量 | D1-PILOT、阈值策略、适用性、指标脚本和统计方法 |
| 性质 | 探索性区分度评价 |
| 结果载体 | 配对差值表、95% 区间、排序表、天花板/地板图和失败样本表 |
| 否证条件 | 多数主指标在对象间无差异、方向反常或排序随小变化反转 |

### 12.3 比较规则

1. 所有 SUT 使用相同样本清单；
2. 主分析单位为源视频或来源组；
3. 报告原始指标、配对差值和聚类 bootstrap 区间；
4. 阈值无关指标与阈值相关指标分开解释；
5. 不适用指标不填零，也不参与平均；
6. 对显著差异同时检查实际效应大小和失败样本；
7. 发现数据捷径或单一来源主导时，先修正数据问题，不把差异归因于 SUT 能力。

### 12.4 E3 结论类型

每个指标只能得到以下建议之一：

- 保留为主指标候选；
- 保留为门槛候选；
- 降为诊断指标；
- 需要更多数据或更强对照；
- 定义/实现需修订；
- 删除候选。

本轮不得根据少数 SUT 的分位数设置等级切点。

## 13. E4 样本量稳定性实验

### 13.1 实验问题

当独立来源组数量增加时，主指标置信区间、SUT 排序、失败类型占比和候选结论是否趋于稳定？

### 13.2 控制设计

| 项目 | 设计 |
|---|---|
| 基线 | D1-PILOT 的最大可用独立来源组结果 |
| 单一变化因素 | 抽样规模 |
| 固定变量 | SUT、分层结构、随机种子列表、指标脚本和区间方法 |
| 性质 | 探索性样本量分析 |
| 结果载体 | 样本量—区间宽度曲线、排序稳定率、结论变化表 |
| 否证条件 | 增加样本后区间和排序仍无稳定趋势，或被单一来源组支配 |

### 13.3 抽样规模

在数据允许时，建议使用 `n=50、100、200、400` 个独立来源组，并以最大可用规模作为参考。每个规模进行至少 30 次分层重复抽样；若某档位不能保持主要分层，则减少分层维度或报告该档位不可解释，不得把相关帧补成独立样本。

### 13.4 稳定性输出

- 每个规模的指标均值、中位数、95% 区间和区间宽度；
- 各 SUT 相对最大规模排序的一致率；
- 主结论和指标角色的变化次数；
- 不同来源、方法族和身份组对总方差的贡献；
- 达到候选精度目标所需的外推样本量；
- 因子组样本不足造成的不可判定项。

### 13.5 停止规则

E4 在预注册的最大规模和重复次数完成后停止，不为获得更好曲线临时增加或删除样本。若最大规模仍未稳定，结论应是“当前样本不足”，并据此设计下一轮数据扩充，不得宣布稳定。

## 14. E0—E4 总运行矩阵

| 实验 | 阶段 | 数据 | SUT/基线 | 变化因素 | 固定项 | 主要输出 | 进入下一阶段的门 |
|---|---|---|---|---|---|---|---|
| E0 | D0 | D0-FIXTURE | 7 类合成基线 | 预测模式 | 真值、脚本 | 预期值对照和回归测试 | 全部核心用例通过 |
| E1 | D0 | D0-SMOKE | 3—5 类 SUT/对照 | 能力形态 | 数据、接口 | 可计算性矩阵 | 指标路由完整，无静默失败 |
| E2 | D1 | D1-PILOT 子集 | 正式 SUT | 运行编号 | 数据、环境、参数 | 哈希和重复性表 | 确定性一致；随机性可量化 |
| E3 | D1 | 同一 D1-PILOT | 基线和正式 SUT | SUT | 数据、阈值、脚本 | 配对差值和区分度表 | 至少部分主指标有可解释区分度 |
| E4 | D1 | 分层规模子集 | 冻结正式 SUT | 独立来源组数 | SUT、分层、脚本 | 稳定曲线和样本量建议 | 给出稳定或不足的明确结论 |

## 15. 预先设计的结果表

### 15.1 E0 预期值对照表

| case_id | 指标 | 真值摘要 | 预测模式 | 人工预期 | 程序结果 | 容差 | 状态 |
|---|---|---|---|---:|---:|---:|---|
|  |  |  |  |  |  |  | pass/fail |

### 15.2 E1 可计算性矩阵

| 指标 | SUT-V1 | SUT-V2 | SUT-AV1 | SUT-W1 | SUT-C1 | 失败原因 | 修订位置 |
|---|---|---|---|---|---|---|---|
|  | value/unsupported/N/A/error |  |  |  |  |  |  |

### 15.3 E2 重复性表

| SUT | 指标 | run-1 | run-2 | run-3 | 均值 | 标准差 | 极差 | 输出哈希一致 | 解释 |
|---|---|---:|---:|---:|---:|---:|---:|---|---|
|  |  |  |  |  |  |  |  |  |  |

### 15.4 E3 区分度表

| 指标 | SUT-A | SUT-B | 配对差值 | 95%区间 | 排序 | 天花板/地板 | 实际解释 | 建议角色 |
|---|---:|---:|---:|---|---|---|---|---|
|  |  |  |  |  |  |  |  |  |

### 15.5 E4 稳定性表

| 指标 | n | 重采样次数 | 中位数 | 95%区间宽度 | 排序一致率 | 结论变化率 | 建议下一规模 |
|---|---:|---:|---:|---:|---:|---:|---:|
|  |  |  |  |  |  |  |  |

### 15.6 问题回写表

| issue_id | 实验 | 现象 | 证据 | 原因类别 | 影响指标/任务 | 回写文件 | 建议动作 | 状态 |
|---|---|---|---|---|---|---|---|---|
|  |  |  |  | 数据/接口/指标/统计/SUT |  |  |  | open/closed |

## 16. 预先设计的图

| 图 | 输入 | 回答的问题 | 禁止误用 |
|---|---|---|---|
| 指标计算回归图 | E0 预期值与结果 | 实现是否偏离预期 | 不能代表真实 SUT 性能 |
| 可计算性热图 | E1 指标×SUT 状态 | 哪些指标依赖哪些输出 | 不把不适用涂成零分 |
| 运行间差异图 | E2 多次运行 | 波动来自哪些指标或对象 | 不只展示均值 |
| 配对差值森林图 | E3 配对差值和区间 | 差异大小和不确定性 | 不用单一 p 值替代效应大小 |
| 样本量—区间宽度曲线 | E4 多规模重采样 | 何时开始稳定 | 不把相关帧当独立样本 |
| 排序稳定图 | E4 排序一致率 | 模型排序是否依赖样本规模 | 不将少量 SUT 排名当等级阈值 |

## 17. 统计与计算规则

### 17.1 分析单位

| 任务 | 主分析单位 | 不得作为独立单位 |
|---|---|---|
| 视频分类 | 源视频/来源组 | 同一视频的帧和裁剪 |
| 音视频分类 | 源视频/来源组 | 同一视频的音频与视觉轨道 |
| 时间定位 | 含伪造片段的源视频 | 单个候选片段 |
| 空间定位 | 源视频或预注册帧 | 同一对象的连续预测框 |
| 重复性 | SUT×冻结运行 | 单样本推理批次 |
| 样本量稳定性 | 来源组 | 扰动副本和近重复样本 |

### 17.2 置信区间

- 比例指标使用 Wilson 区间作基础检查；
- F1、AUC、AP、IoU、tIoU 和配对差值使用来源组层级的分层 bootstrap；
- 正式探索报告建议至少 2000 次 bootstrap；E4 内层重复抽样与指标区间计算应区分，避免把两层随机性混为一层；
- 每次重采样保留主要真伪、来源和方法族分层；
- 区间方法、随机种子列表和实现版本写入运行记录。

### 17.3 阈值策略

E0 使用为测试用例预设的固定阈值。E1—E4 优先使用 SUT 冻结阈值或在独立于 D1 评价样本的开发数据上确定的阈值。不得在 D1-PILOT 上为每个指标反复搜索最优阈值后再报告同一数据结果。

### 17.4 多重比较

E3 预先指定每类能力的主指标，其余作为诊断指标。报告全部计划比较和结果，不从大量 SUT、子组和指标中只选择有利差异。首轮重点是区分度和实际效应，不以 `p<0.05` 作为保留指标的唯一依据。

## 18. 运行目录与不可变归档

```text
experiments/mvp/
├─ README.md
├─ schemas/
├─ vocabularies/
├─ fixtures/
│  ├─ ground_truth/
│  └─ predictions/
├─ manifests/
│  ├─ D0-FIXTURE/
│  ├─ D0-SMOKE/
│  └─ D1-PILOT/
├─ systems/
│  ├─ registry/
│  ├─ adapters/
│  └─ configs/
├─ metrics/
│  ├─ definitions/
│  ├─ calculators/
│  └─ tests/
├─ runs/
│  └─ <run_id>/
│     ├─ run-manifest.json
│     ├─ raw-output/
│     ├─ normalized-output.jsonl
│     ├─ logs/
│     └─ checksums.txt
├─ analysis/
│  ├─ tables/
│  ├─ figures/
│  └─ notebooks/
└─ reports/
   ├─ E0-report.md
   ├─ E1-report.md
   ├─ E2-report.md
   ├─ E3-report.md
   ├─ E4-report.md
   └─ issue-backwrite.md
```

正式运行目录不得覆盖。重跑应产生新的 `run_id`，并通过父运行编号说明原因。媒体数据可位于受控外部存储中，实验目录只保存相对标识、访问记录和哈希，不因方便而复制受限数据。

## 19. 实施阶段与工作包

### 19.1 WP0：许可、资源和范围冻结

**输入**：本方案、候选数据和候选 SUT。

**任务**：

- 完成许可、访问、安全和存储审查；
- 确认可用 CPU/GPU、磁盘和执行人员；
- 冻结首轮 SUT 能力形态与替代对象规则；
- 冻结 D0/D1 的目标规模和抽样策略；
- 建立风险、问题和决策日志。

**输出**：数据许可登记、资源清单、首轮范围冻结单。

### 19.2 WP1：Schema、词表与合成测试

**任务**：实现首批七类 Schema、受控词表和 D0-FIXTURE；人工计算 E0 预期值；建立自动校验和单元测试。

**输出**：Schema v1.0.0、E0 用例、预期值表和回归测试。

### 19.3 WP2：数据清单与适配器

**任务**：建立 D0-SMOKE 和 D1-PILOT 清单；完成来源组与去重；为 SUT-V1、V2、AV1、W1 和 C1 实现统一输出适配。

**输出**：不可变清单、哈希、适配器和样例结果。

### 19.4 WP3：E0—E1 执行

**任务**：先执行 E0；通过后运行 E1；修复计算或接口问题并全量回归；冻结可计算性矩阵。

**输出**：E0/E1 报告、缺陷清单和 Q1 进入意见。

### 19.5 WP4：E2—E4 执行

**任务**：冻结运行条件；完成重复性、区分度和样本量稳定性实验；生成预设计表图；开展失败样本分析。

**输出**：E2—E4 报告、指标角色建议和数据扩充建议。

### 19.6 WP5：回写与评审

**任务**：将发现映射到 02、03、07、08、09 和标准框架；由指标、数据、实验和标准化人员联合复核。

**输出**：问题回写表、修订建议和下一轮 E5/E6 预注册输入。

## 20. 进度建议与依赖关系

| 周次 | 工作包 | 关键输出 | 进入条件 |
|---|---|---|---|
| 第 1 周 | WP0 | 许可、资源、SUT 和范围冻结 | 候选来源完成初审 |
| 第 2 周 | WP1 | Schema、词表、D0-FIXTURE 和 E0 测试 | 字段定义无重大歧义 |
| 第 3 周 | WP2 | D0/D1 清单、去重报告和适配器 | 数据访问与环境可用 |
| 第 4 周 | WP3 | E0/E1 报告与问题修复 | E0 全部核心用例通过 |
| 第 5 周 | WP4 前半 | E2/E3 原始结果和初步分析 | E1 指标路由完整 |
| 第 6 周 | WP4 后半、WP5 | E4、总报告和回写建议 | E2/E3 运行冻结 |

时间表是资源规划基线。许可审批、数据获取或模型部署造成的等待应记录为外部依赖，不应通过跳过许可或降低验证要求追赶时间。

## 21. 角色与责任

| 角色 | 主要职责 | 审批/复核 |
|---|---|---|
| 实验负责人 | 冻结问题、变量、运行矩阵和停止规则 | 批准阶段进入 |
| 数据管理员 | 许可、清单、分区、来源组、哈希和访问 | 数据门 DG1—DG5 |
| Schema/指标开发人员 | Schema、校验器、计算器和单元测试 | E0 正确性 |
| SUT 适配人员 | 环境、权重、适配器和能力声明 | 输出语义与日志 |
| 实验执行人员 | 按运行清单执行，不修改冻结项 | 原始结果和运行记录 |
| 统计分析人员 | 区间、配对、重采样和稳定性分析 | 表图和统计口径 |
| 独立复算人员 | 使用只读结果包复算关键结果 | 可复现性签字 |
| 标准编写人员 | 把问题和证据回写条款 | 规范语言和证据边界 |

同一人员可以兼任多个技术角色，但 E0 人工预期、关键结果复算和排除样本审批不宜由唯一一人包办。

## 22. 阶段门与回退

| 门 | 通过条件 | 失败处理 |
|---|---|---|
| P0 许可与资源 | 数据、SUT 和环境均可合法、稳定使用 | 替换来源/SUT；仅继续 E0 |
| P1 Schema 完整 | 示例、边界和必填逻辑通过验证 | 修订 09 和 Schema |
| P2 E0 正确 | 核心用例与人工预期一致 | 修复计算器并全量回归 |
| P3 E1 可执行 | 39 项均有明确路由，无静默失败 | 修订接口、适用性或指标定义 |
| P4 E2 可复现 | 确定性一致，随机波动可量化 | 固定环境/随机源或增加重复 |
| P5 E3 可区分 | 至少部分主指标有可解释差异 | 增加对照、修复数据或降级指标 |
| P6 E4 可决策 | 得到“趋稳”或“样本不足”的明确结论 | 扩充 D1，不进入 D2 |
| P7 回写闭环 | 每项问题有责任文件和处理状态 | 不启动 E5/E6 正式先导 |

阶段门不是为了保证结果“好看”，而是为了防止错误链路、许可不清、数据泄漏或不稳定指标进入后续校准。

## 23. 风险与控制措施

| 风险 | 可能后果 | 早期信号 | 控制措施 |
|---|---|---|---|
| 外部数据条款不兼容 | 无法合法使用或发布结果 | 许可范围含糊、需额外同意 | P0 前完成书面审查，准备替代数据 |
| 官方模型依赖老旧 | 部署耗时或结果漂移 | 包冲突、权重无法加载 | 独立容器、锁定提交、保留替代 SUT |
| 底层数据跨库重复 | 泛化和区分度被高估 | 感知哈希或身份重合 | 来源组去重和污染标记 |
| 只覆盖人脸视觉 | A/B 能力验证偏窄 | M4、O2—O4 为空 | 诚实标记缺口，用受控 D0 验证接口 |
| 39 项指标首轮全部展开过重 | 实现质量下降 | 大量半成品计算器 | E0 优先核心公式，仍为全部指标建立路由 |
| 适配器改变模型语义 | 对象间不可比 | 阈值、分数方向不一致 | 原始与标准化输出并存，适配器单测 |
| 样本独立性不足 | 区间虚假变窄 | 同身份/源视频集中 | `group_id` 聚类重采样 |
| 参测对象太少 | 区分度结论脆弱 | 排名由单个模型决定 | 只作探索结论，不设正式切点 |
| 计算和分析反复修改 | 选择性报告 | 结果后更换主指标 | 预先设计表图、版本化重跑 |
| 运行目录被覆盖 | 无法审计和复现 | 同名结果时间戳变化 | 不可变 `run_id` 和全包哈希 |

## 24. 预注册清单

执行 E2—E4 前至少冻结：

- 数据版本、样本清单、来源组和排除规则；
- SUT、权重、配置、阈值、适配器和能力声明；
- 运行环境、硬件、并发、预热和计时边界；
- 正类定义、分析单位和适用性矩阵；
- E2 重复次数和随机种子；
- E3 主指标、对照和配对比较；
- E4 抽样规模、重复次数、分层和停止规则；
- 置信区间、bootstrap 和多重比较处理；
- 失败、超时、缺失、不适用和排除样本处理；
- 结果表、图、否证条件和阶段门；
- 允许修改的范围及修改后是否全量重跑。

## 25. 产出清单

### 25.1 机器可执行产出

1. 七类 JSON Schema；
2. 受控词表和字段说明；
3. D0-FIXTURE、D0-SMOKE 和 D1-PILOT 清单；
4. 被测对象登记与统一输出适配器；
5. 指标计算器和 E0 回归测试；
6. E2—E4 统计分析脚本；
7. 不可变运行目录、日志和哈希。

### 25.2 研究与标准产出

1. E0 计算正确性报告；
2. E1 指标可计算性报告；
3. E2 重复性报告；
4. E3 基础区分度报告；
5. E4 样本量稳定性报告；
6. 失败样本和问题回写表；
7. 指标保留、降级、修订或删除建议；
8. E5 扰动强度和 E6 四象限泛化的先导设计输入。

## 26. 官方来源记录

本方案的数据与框架候选仅依据官方仓库和论文入口核验：

1. [DeepfakeBench 官方仓库](https://github.com/SCLBD/DeepfakeBench)：统一数据管理、检测器实现和评测协议；仓库声明 CC BY-NC 4.0，同时明确聚合数据版权属于原始提供方。
2. [DF40 官方仓库](https://github.com/YZY-stack/DF40)：多方法视觉伪造候选；官方仓库声明数据与代码受 CC BY-NC 4.0 约束。
3. [LAV-DF 官方仓库](https://github.com/ControlNet/LAV-DF)：音视频伪造与时间定位候选；下载前需要同意使用条款，项目声明 CC BY-NC 4.0。
4. [AV-Deepfake1M 官方仓库](https://github.com/ControlNet/AV-Deepfake1M)：大规模音频、视觉和联合伪造扩展候选；数据访问需签署 EULA，项目不同部分适用不同许可。

引用这些来源不代表数据已经获得或许可审查已经完成。实际使用时应保存当时的许可版本、访问同意和下载清单。

## 27. 下一步工作

本方案通过审查后，下一步应实施：

> `11-实验数据Schema与E0校验工具实施说明.md`

该阶段首先创建 `experiments/mvp/` 目录，完成首批 JSON Schema、受控词表、D0-FIXTURE 和 E0 人工预期值，再编写校验器和指标回归测试。只有 E0 全部核心用例通过后，才进入外部数据获取和 E1 冒烟运行。
